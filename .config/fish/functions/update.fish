function update
    echo "🚀 Починаємо оновлення системи..."

    # Оновлюємо AUR та системні пакети через yay
    echo "🔄 Оновлення через yay..."
    yay -Syu --noconfirm

    # Оновлюємо Flatpak пакети
    echo "🔄 Оновлення Flatpak..."
    flatpak update -y

    # Оновлюємо плагіни Fisher
    if fisher list >/dev/null
        echo "🔄 Оновлення плагінів Fisher..."
        fisher update
    else
        echo "⚠️ Fisher не знайдено. Пропускаємо..."
    end

    echo "✅ Оновлення завершено!"
end
